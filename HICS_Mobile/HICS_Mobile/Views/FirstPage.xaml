<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:HICS_Mobile.ViewModels"
             x:Class="HICS_Mobile.Views.FirstPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="237*"/>
                <RowDefinition Height="350*"/>
                <RowDefinition Height="501*"/>
            </Grid.RowDefinitions>
            <Button Text="Hit CreatePushRegistrationId" Command="{Binding GetRegistrationIdCommand}"></Button>
            <!--<Button Text="Hit UnregisterFromNotifications" Command="{Binding UnregisterFromNotifications}" ></Button>
            <Button Text="Hit RegisterForPushNotifications" Command="{Binding RegisterForPushNotifications}" ></Button>
            <Button Text="Hit SendNotification" Command="{Binding SendNotification}" ></Button>-->
        </Grid>
    </ContentPage.Content>
</ContentPage>